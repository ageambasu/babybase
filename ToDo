/**
 * Scope 1: Short term
 */
-Move layout.blade & users.edit.blade js to app.js

/**
 * Scope 2: Mid term
 */
-Fix age_today into numeric value for filtering at public function age_today($value = null)
-Fix study filter works as OR, should work as AND
-Fix filtering as LIKE instead of =, and show full list of values for multiselect
-Participant form confirm showed fields, and submit with confirmation message or redirect to specified url (when user not logged in)

/**
 * Scope 3: Long term
 */
-Create languages table?
-Create appointments table?
-Pivot baby_note_study + drop studies.note?
-Modify Filter to include pivot tables?
-Fix enable admin option during account creation?